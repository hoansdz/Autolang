#ifndef STACKALLOCATOR_CPP
#define STACKALLOCATOR_CPP

#include "StackAllocator.hpp"
#include "Interpreter.hpp"

/*void StackAllocator::set(AVM* vm, size_t index, AObject* object) {
	
}

void StackAllocator::freeTo(size_t top) {
	
}

void StackAllocator::clear(AVM* vm, size_t from, size_t to) {
	
}*/

#endif