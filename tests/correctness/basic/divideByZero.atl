fun testDivideByZero() {
	// Int divide by zero -> exception
	var errorCaught = false
	try {
		val a = 10
		val zero = 0
		val b = a / zero
	} catch (e) {
		errorCaught = true
	}
	assert(errorCaught)

	// Int modulo by zero -> exception
	errorCaught = false
	try {
		val a = 10
		val zero = 0
		val b = a % zero
	} catch (e) {
		errorCaught = true
	}
	assert(errorCaught)

	// Exception in try block doesn't affect code after catch
	var afterCatch = 0
	try {
		val zero = 0
		val a = 5 / zero
	} catch (e) {
		afterCatch = 42
	}
	assert(afterCatch == 42)

	println("Passed ${__FUNC__}")
}

testDivideByZero()