@import("std/vm")

func testVmFeatures() {
    val count = VM.getCountAreaAllocatorObject()
    // It should return the number of active objects in the VM area allocator (a non-negative integer)
    assert(count >= 0, __FILE__, __LINE__)

    println("Passed ${__FUNC__}")
}

testVmFeatures()
