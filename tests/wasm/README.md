# WebAssembly Integration Tests (`tests/wasm`)

This directory is dedicated to compiling and running AutoLang inside WebAssembly (Wasm) targets.

## Key Files

- **`main.cpp`**: Direct compilation harness for evaluating AutoLang interpreter execution and performance under WASM build configurations (using Emscripten).
