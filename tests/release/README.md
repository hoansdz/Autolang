# Release Build Verification (`tests/release`)

This directory is designed for running end-to-end integration and verification tests under release builds of AutoLang.

## Key Files

- **`main.cpp`**: Prepares and launches compilation/execution tests targeting release builds to verify compiler optimization flags, speed, and behavior safety.
